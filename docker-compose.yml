version: "3.8"

services:
  db:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: ecom
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3306:3306"
    volumes:
      - my-db:/var/lib/mysql

volumes:
  my-db: